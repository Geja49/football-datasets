# Copier vers .env et renseigner si vous avez une cle.
# Ne jamais committer .env.

# API-Football (api-sports.io). Sans cle, le collecteur ne fait rien.
CLE_API_FOOTBALL=

# The Odds API (the-odds-api.com). Sans cle, la page Cotes affiche le calendrier sans cotes.
CLE_API_COTES=

# Valeurs de marche : pas de cle. Dump tiers CC0 (transfermarkt-datasets /
# Kaggle davidcariboo/player-scores) via scripts/collecter_valeurs_marche.py.
# Zone grise (donnees issues de Transfermarkt) ; jamais de scrape live.

# Compte admin communaute (commentaires). Optionnel : l'inscription avec cet e-mail devient admin.
EMAIL_ADMIN_COMMUNAUTE=

# Connexion Google (OAuth 2.0). Optionnel : sans GOOGLE_CLIENT_ID, seul l'e-mail/mdp est actif.
# Creer un identifiant OAuth « Application Web » dans Google Cloud Console
# et autoriser l'URI de redirection JavaScript (origines autorisees du site).
GOOGLE_CLIENT_ID=

